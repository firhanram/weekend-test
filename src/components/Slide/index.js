const Slide = ({by, testimony}) => (
    <div className="bg-white p-5">
        <h2 className="font-black text-3xl mb-5">{by}</h2>
        <p className="text-xs lg">{testimony}</p>
    </div>
)

export default Slide 